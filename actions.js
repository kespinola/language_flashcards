import { createAction } from 'redux-actions';

export const addCorrect = createAction('ADD_CORRECT');

export const nextCard = createAction('NEX_CARD');

export const prevCard = createAction('PREV_CARD');

export const changeLang = createAction('CHANGE_LANG');

